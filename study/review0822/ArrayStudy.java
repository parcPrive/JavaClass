package review0822;

import java.util.Arrays;

import javax.swing.JOptionPane;

public class ArrayStudy {
  public static void main(String[] args) {
    // int aaa[] = {1,2};
    // int bbb[] = aaa;
    // bbb[1] = 3;
    // System.out.println(aaa);
    // System.out.println(bbb);
    // System.out.println(Arrays.toString(aaa));
    // System.out.println(Arrays.toString(bbb));

    // int ball[] = new int[45];
    // for(int i = 0; i < ball.length; i++){
    //   ball[i] = i + 1;
    // }
    // int temp = 0;
    // int j = 0;
    // for(int i = 0; i < 100; i++){
    //   j = (int) (Math.random() * 45);
    //   temp = ball[0];
    //   ball[0] = ball[j];
    //   ball[j] = temp;
    // }
    // for(int i = 0; i < 6; i++){
    //   System.out.println(ball[i]);
    // }

    // int aaa[][] = new int[5][4];

    // for(int i = 0; i < aaa.length; i++){
    //   for(int j = 0; j < aaa[i].length; j++){
    //     aaa[i][j] = j;
    //   }
    // }
    // for(int i = 0; i < aaa.length; i++){
    //   System.out.println(Arrays.toString(aaa[i]));

    // }

    // int aaa[] = {1,2,3,4,5};
    // int bbb[] = new int[10];
    // // for(int i = 0; i < aaa.length; i++){
    // //   bbb[i] = aaa[i];
    // // }
    // System.arraycopy(aaa, 0 ,bbb , 0, aaa.length);
    // System.out.println(Arrays.toString(bbb));

    // int answer = (int) (Math.random() * 100 +1);
    
    // String temp = "";
    // int count = 0;

    // while(true){
    // if(count == 0){
    //   temp = JOptionPane.showInputDialog("1 ~ 100사이의 숫자를 입력하세요. 끝내려면 -1을 입력해수세요.");
    // }else if(Integer.parseInt(temp) > answer){
    //   temp = JOptionPane.showInputDialog(temp + "보다 작습니다.");
    // }else{
    //   temp = JOptionPane.showInputDialog(temp + "보다 큽니다.");
    // }
    // count++;
    // if(temp.equals("-1")) break;
    // if(answer == Integer.parseInt(temp)){
    //   System.out.println("정답입니다." + count + "번만에 맞추셨습니다.");
    //   break;
    // }
    // }


    
  }
}
